# web-project

1. Clone this repo

    ```shell
    $ git clone https://github.com/duclm278/web-project.git
    ```

2. Install dependencie

    ```shell
    $ cd frontend
    $ npm install
    $ cd ../backend
    $ npm install
    ```
